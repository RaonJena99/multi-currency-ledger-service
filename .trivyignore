# 이미지 취약점 스캔(Trivy)에서 제외할 항목. CI 와 릴리스 워크플로가 함께 사용한다.
#
# 한 줄에 취약점 ID 하나. 반드시 위에 "왜 이 앱에서 악용할 수 없는지"와 "언제 다시 볼지"를 적는다.
# 가능하면 제외하지 말고 의존성 버전을 올린다(Dependabot PR 또는 build.gradle).
#
# 예)
# # tomcat-embed-core: HTTP/2 전용 경로. 이 앱은 HTTP/2 를 켜지 않는다. Boot 4.0.9 에서 제거 예정.
# CVE-2026-00000
